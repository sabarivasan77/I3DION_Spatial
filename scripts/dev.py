import subprocess
import os

if __name__ == "__main__":
    print("Use 'python start.py' from root to start all services.")
    print("If you only want to start the frontend, run 'npm run dev' inside the 'frontend' directory.")
    print("If you only want to start the backend, run 'npm run dev' inside the 'backend' directory.")
